mixin image(path, className, description, attrRole)
  if role
    img(src=require(`../../assets/img/${path}`) class=className role= attrRole)
  else
    img(src=require(`../../assets/img/${path}`) class=className alt=description)




